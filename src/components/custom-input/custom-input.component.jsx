import React from "react";

import "./custom-input.styles.scss";

const CustomInput = ({ name, placehoder }) => {
  return (
    <div className="custom-input">
      <input type="text" name="" id="" placeholder={placehoder} />
    </div>
  );
};

export default CustomInput;
